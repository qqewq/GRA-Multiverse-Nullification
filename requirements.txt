# requirements.txt
# GRA-Multiverse-Nullification v1.0 (February 2026)
# PyTorch implementation of multilevel meta-nullification architecture
# See: https://github.com/qqewq/GRA-Multiverse-Nullification

# Core numerical computing
torch>=2.1.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Scientific computing (optional quantum backend)
qutip>=4.7.0,<5.0.0
scipy>=1.11.0,<2.0.0

# Documentation & LaTeX
matplotlib>=3.7.0,<4.0.0
pillow>=10.0.0,<11.0.0

# Development & testing
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0
black>=23.11.0,<24.0.0
mypy>=1.7.0,<2.0.0
isort>=5.12.0,<6.0.0

# Performance visualization
seaborn>=0.13.0,<0.14.0

# Type checking & linting
types-torch>=0.15.0
types-numpy>=1.24.0.20231201

# LaTeX compilation (optional, for main.tex)
# Install separately: texlive-full (Ubuntu/Debian) or MacTeX (macOS)

# Pin exact versions for reproducibility (uncomment for production):
# torch==2.1.2
# numpy==1.26.4
# qutip==4.7.1
# matplotlib==3.8.4
# pytest==7.4.4

# Development installation:
# pip install -r requirements.txt
# pip install -e .[dev]

# Notes:
# - CUDA support: torch auto-detects GPU
# - Virtual environment recommended: python -m venv .venv
# - Generate lockfile: pip freeze > requirements.lock
